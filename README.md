Vagrant VM that runs clean installs of dry components, and tests that they work.

To run:

clone repo.

run ```vagrant up```
run ```./test```

That's it.

Want to add tests? Check out the /test-scripts folder, and the vagrant-test-start.sh script
